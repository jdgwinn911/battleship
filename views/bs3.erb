<html>
    <head>
        <script src="/js/bs.js"></script>
        <link rel="stylesheet" type="text/css" href="css/bs3.css">
    </head>
    <body onload="removeElement()">
        <center>
         <form method="post" action="page3">
            <div class="player1">
            <table id="mytable">
                <tr>
                <th> </th>
                <%board.grid.size.times do |v|%>
                <th><%=v%></th>
                <%end%>
                </tr>
           
                <%board.grid.each_with_index do |v, i|%>
                    <tr>
                    <th><%=i%></th>
                        <%v.each_with_index do |k, o|%>
                            <th><%=k%></th>
                        <%end%>
                    </tr>
                <%end%>    

            </table>            
            </div>
            <div class="enemyboard">
            <table id="enemytable">
                <tr>
                <th> </th>
                <%enemyboard.grid.size.times do |v|%>
                <th><%=v%></th>
                <%end%>
                </tr>
           
                <%enemyboard.grid.each_with_index do |v, i|%>
                    <tr>
                    <th><%=i%></th>
                        <%v.each_with_index do |k, o|%>
                            <th><%=k%></th>
                        <%end%>
                    </tr>
                <%end%>    
                </div>
            

            </table>            

            <div class="coordinates">
            <font color="red"><h3> row coordinate</h3></font>
                <input type="number" name="row" placeholder="row">
                 <font color="red"><h3>column coordinate</h3></font>
                <input type="number" name="col" placeholder="column">
                 <font id="HV" color="red"><h3>horizontal or vertical</h3></font>
                <input type="text" name="pos" placeholder="hor/vert" id="pos">
                <input type='hidden' name='ship_num' value='<%=ship_num%>' id="ship_num">
                <font id="err"color="red"><h1><%=err%></h1></font>
                <font id="fire_err"color="red"><h1><%=fire_err%></h1></font>

                <br><br>
                <button class=" button">Enter</button>
            </div>

        </center>

       
        </form>

           <div class="stopwatch" id="stopwatch">
                <h1><font color="red"><span id="seconds">00</span>:<span id="tens">00</span></h1></font>
                <button onclick="stopWatch()"id="start">Start</button>
                <button onclick="stopWatch()" id="stop">Stop</button>
                <button onclick="stopWatch()" id="reset">Reset</button>
            </div>


    </body> 

</html>   

<%# <script>
window.onload = function () {
  
    var seconds = 00; 
    var tens = 00; 
    var appendTens = document.getElementById("tens")
    var appendSeconds = document.getElementById("seconds")
    var buttonStart = document.getElementById('start');
    var buttonStop = document.getElementById('stop');
    var buttonReset = document.getElementById('reset');
    var Interval ;
  
    buttonStart.onclick = function() {
      
       clearInterval(Interval);
       Interval = setInterval(startTimer, 10);
    }
    
      buttonStop.onclick = function() {
         clearInterval(Interval);
    }
    
  
    buttonReset.onclick = function() {
       clearInterval(Interval);
      tens = "00";
        seconds = "00";
      appendTens.innerHTML = tens;
        appendSeconds.innerHTML = seconds;
    }
    
     
    
    function startTimer () {
      tens++; 
      
      if(tens < 9){
        appendTens.innerHTML = "0" + tens;
      }
      
      if (tens > 9){
        appendTens.innerHTML = tens;
        
      } 
      
      if (tens > 99) {
        console.log("seconds");
        seconds++;
        appendSeconds.innerHTML = "0" + seconds;
        tens = 0;
        appendTens.innerHTML = "0" + 0;
      }
      
      if (seconds > 9){
        appendSeconds.innerHTML = seconds;
      }
    
    }
    
  
  }
</script> %>









